# 🧪 車手載入重構測試清單

## 📅 測試時間
2025-10-15

## 🎯 測試目標
驗證 API-FIRST 車手載入架構在開發環境和 EXE 環境中正常運作

---

## ✅ **階段 1: 開發環境測試**

### 測試 1.1: API 可用情況
**前置條件**: 確保 API 服務器正在運行
```powershell
# 啟動 API 服務器（如果尚未啟動）
python refactored_api.py
```

**執行步驟**:
1. 啟動 F1T GUI: `python f1t_gui_main.py`
2. 點擊 **單圈分析 → 速度分析**
3. 年份選擇: `2025` 
4. 賽事選擇: `Japan`

**預期結果**:
- [ ] Console 顯示 `[DRIVERS] 步驟 1: 優先嘗試 API 調用 (F98 端點)...`
- [ ] Console 顯示 API 基礎 URL（本地: `http://localhost:8000` 或公開: `https://api...`）
- [ ] Console 顯示 `[DRIVERS] ✅ 透過 API 成功獲取 XX 個車手`
- [ ] **車手 1 下拉選單**: 顯示 21 個車手（VER, NOR, LEC, PIA...）
- [ ] **車手 2 下拉選單**: 顯示 "None" + 21 個車手

---

### 測試 1.2: API 不可用情況（JSON 備用）
**前置條件**: 停止 API 服務器
```powershell
# 停止 API 服務器（或關閉終端）
```

**執行步驟**:
1. 啟動 F1T GUI: `python f1t_gui_main.py`
2. 點擊 **單圈分析 → 速度分析**
3. 年份選擇: `2025`
4. 賽事選擇: `Japan`

**預期結果**:
- [ ] Console 顯示 `[DRIVERS] ⚠️  API 調用失敗: HTTPConnectionPool...` 或類似錯誤
- [ ] Console 顯示 `[DRIVERS] 步驟 2 (備用策略): 從本地 JSON 讀取...`
- [ ] Console 顯示 `[DRIVERS] ✅ 從 JSON 提取到 XX 個車手`
- [ ] **車手 1 下拉選單**: 顯示 21 個車手
- [ ] **車手 2 下拉選單**: 顯示 "None" + 21 個車手

---

### 測試 1.3: JSON 也不可用（硬編碼備用）
**前置條件**: 
1. 停止 API 服務器
2. 臨時重命名 `json/` 資料夾
```powershell
Rename-Item -Path "json" -NewName "json_backup_test"
```

**執行步驟**:
1. 啟動 F1T GUI: `python f1t_gui_main.py`
2. 點擊 **單圈分析 → 速度分析**
3. 年份選擇: `2025`
4. 賽事選擇: `Japan`

**預期結果**:
- [ ] Console 顯示 `[DRIVERS] ⚠️  API 調用失敗...`
- [ ] Console 顯示 `[DRIVERS] ⚠️  讀取 JSON 失敗...`
- [ ] Console 顯示 `[DRIVERS] 步驟 3 (最終備用): 使用 2025 年標準車手列表`
- [ ] Console 顯示 `[DRIVERS] ✅ 備用車手列表已載入: 21 個車手`
- [ ] Console 顯示 `[DRIVERS] 💡 提示: 如需最新車手數據，請確保 API 服務正常運行`
- [ ] **車手 1 下拉選單**: 顯示 21 個車手
- [ ] **車手 2 下拉選單**: 顯示 "None" + 21 個車手

**測試後清理**:
```powershell
# 還原資料夾名稱
Rename-Item -Path "json_backup_test" -NewName "json"
```

---

## ✅ **階段 2: EXE 環境測試**

### 測試 2.1: 重新生成 EXE
```powershell
# 清理舊的建置檔案
Remove-Item -Recurse -Force build, dist -ErrorAction SilentlyContinue

# 執行 PyInstaller
pyinstaller F1T_GUI.spec --clean
```

**預期結果**:
- [ ] 建置完成無錯誤
- [ ] `dist/F1T_GUI.exe` 已生成（約 192 MB）

---

### 測試 2.2: EXE - API 可用情況
**前置條件**: 確保 API 服務器正在運行

**執行步驟**:
1. 執行 `dist\F1T_GUI.exe`
2. 點擊 **單圈分析 → 速度分析**
3. 年份選擇: `2025`
4. 賽事選擇: `Japan`

**預期結果**:
- [ ] **車手 1 下拉選單**: 顯示 21 個車手（VER, NOR, LEC...）
- [ ] **車手 2 下拉選單**: 顯示 "None" + 21 個車手
- [ ] ❌ **不再出現**: 車手 1 空白、車手 2 只有 None

**調試方法**（如果失敗）:
- 檢查 EXE 所在目錄的 `driver_loading_error.log` 檔案
- 使用 Process Monitor 查看檔案存取路徑

---

### 測試 2.3: EXE - API 不可用（JSON 備用）
**前置條件**: 停止 API 服務器

**執行步驟**:
1. 確保 `json/` 資料夾在 EXE 同目錄（或複製到 EXE 目錄）
2. 執行 `dist\F1T_GUI.exe`
3. 點擊 **單圈分析 → 速度分析**
4. 年份選擇: `2025`
5. 賽事選擇: `Japan`

**預期結果**:
- [ ] **車手 1 下拉選單**: 顯示 21 個車手
- [ ] **車手 2 下拉選單**: 顯示 "None" + 21 個車手
- [ ] `driver_loading_error.log` 包含 "步驟 2 (備用策略)" 訊息

---

### 測試 2.4: EXE - 完全離線（硬編碼備用）
**前置條件**: 
1. 停止 API 服務器
2. 將 EXE 移到沒有 `json/` 資料夾的乾淨目錄

**執行步驟**:
1. 執行 EXE（新位置）
2. 點擊 **單圈分析 → 速度分析**
3. 年份選擇: `2025`
4. 賽事選擇: `Japan`

**預期結果**:
- [ ] **車手 1 下拉選單**: 顯示 21 個車手（硬編碼列表）
- [ ] **車手 2 下拉選單**: 顯示 "None" + 21 個車手
- [ ] `driver_loading_error.log` 包含 "步驟 3 (最終備用)" 訊息

---

## ✅ **階段 3: 日誌驗證**

### 測試 3.1: Console 輸出檢查（開發環境）
**執行步驟**:
1. 啟動 GUI 並打開速度分析對話框
2. 複查終端輸出

**必須包含的日誌行**:
- [ ] `[DRIVERS] 開始載入可用車手列表...`
- [ ] `[DRIVERS] 年份: XXXX, 賽事: XXXX, 會話: X`
- [ ] `[DRIVERS] 步驟 1: 優先嘗試 API 調用...` 或 `步驟 2 (備用策略)...`
- [ ] `[DRIVERS] ✅ 透過 API 成功獲取...` 或其他成功訊息
- [ ] `[DRIVERS] 車手列表已填充至下拉選單`

### 測試 3.2: 錯誤日誌檢查（EXE 環境）
**執行步驟**:
1. 在 API 失敗情況下執行 EXE
2. 檢查 `driver_loading_error.log` 內容

**必須包含的資訊**:
- [ ] 時間戳記
- [ ] API 錯誤詳情（如果 API 失敗）
- [ ] JSON 讀取失敗原因（如果 JSON 失敗）
- [ ] 最終使用的策略編號（1/2/3）

---

## 📊 **測試結果彙總**

### 開發環境測試
| 測試案例 | 狀態 | 備註 |
|---------|------|------|
| 1.1 API 可用 | ⏳ 待測 | |
| 1.2 JSON 備用 | ⏳ 待測 | |
| 1.3 硬編碼備用 | ⏳ 待測 | |

### EXE 環境測試
| 測試案例 | 狀態 | 備註 |
|---------|------|------|
| 2.1 重新建置 | ⏳ 待測 | |
| 2.2 API 可用 | ⏳ 待測 | |
| 2.3 JSON 備用 | ⏳ 待測 | |
| 2.4 硬編碼備用 | ⏳ 待測 | |

### 日誌驗證
| 測試案例 | 狀態 | 備註 |
|---------|------|------|
| 3.1 Console 輸出 | ⏳ 待測 | |
| 3.2 錯誤日誌 | ⏳ 待測 | |

---

## 🐛 **已知問題追蹤**
（測試過程中發現的問題記錄在此）

---

## 📌 **附註**
- 本測試清單基於 2025-10-15 的 API-FIRST 重構
- 主要變更: 移除 78 行進站 JSON 解析邏輯，簡化為三層策略
- 相關檔案: `f1t_gui_main.py` 行 960-1025

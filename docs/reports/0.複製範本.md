# 🎯 GUI 模組完整功能複製範本



## 📝 使用方式



**在提示詞開頭指定來源和目標模組**：



```markdown

【來源模組】Speed 模組## 💡 **關鍵字總結**

【目標模組】Gear 模組

### **最有效的關鍵字組合**

請執行以下完整功能複製流程...

``````markdown

✅ 強制驗證：

---- "請先用 grep_search 驗證 [方法名] 是否存在"

- "請讀取 Speed 的完整實現（不要省略任何行）"

## 🔧 完整複製流程- "請逐行比對每個 if/else 分支"



請執行以下步驟來完整複製 **[來源模組]** 的所有功能到 **[目標模組]**：✅ 防止跳過：

- "包括所有 else 分支"

---- "包括所有 print 語句"

- "包括異常處理的 try/except"

### 【階段 0：原則宣告】

✅ 關鍵邏輯：

請先宣告「反幻覺編碼五原則」，確認你理解：- "檢查是否有 'if params_changed' → 'else: 跳過重載'"

- "確認是否無條件調用 load_data()"

1. **禁止幻覺編碼** - 必須先驗證再編寫- "驗證 _update_info_label() 的調用時機"

   - ❌ 禁止假設任何方法存在

   - ✅ 每次調用前必須用 `grep_search` 驗證✅ 日誌驅動：

   - "根據日誌差異反向追蹤源代碼"

2. **模組資料夾優先** - 複用現有功能- "解釋為什麼 Speed 輸出 XXX，而 Throttle 輸出 YYY"
   - ✅ 開發前先檢查 `modules/gui/` 是否已有實現
   - ✅ 發現既有功能時必須複用或繼承
   
3. **通用模組優先** - 統一架構模式
   - ✅ 必須使用 `UniversalDataLoader` 作為基礎類別
   - ✅ 參考 Rain Analysis 的標準範本
   
4. **模組多國語言化** - 使用 tr()
   - ✅ 所有用戶可見字串必須用 `tr()` 包裹
   - ❌ 不可有 emoji
   
5. **print 輸出會被 logger 導出**
   - ✅ 所有 print 調用會被導出到 log
   - ✅ 如有調用 print 請查看 logs/gui 日誌

---

### 【階段 1：完整方法列表對比】

#### 1.1 列出來源模組的所有方法

```bash
grep_search: "^    def "
includePattern: "modules/gui/lap_analysis/[來源模組名稱]/[來源模組名稱]_mdi.py"
```

#### 1.2 列出目標模組的所有方法

```bash
grep_search: "^    def "
includePattern: "modules/gui/lap_analysis/[目標模組名稱]/[目標模組名稱]_mdi.py"
```

#### 1.3 生成對比清單

**輸出格式**：
```markdown
【方法對比結果】

✅ 兩者都有的方法（需逐行對比）：
- method_name_1 (來源: Line XXX, 目標: Line YYY)
- method_name_2 (來源: Line XXX, 目標: Line YYY)
...

❌ 來源有但目標沒有的方法（需完整複製）：
- missing_method_1 (來源: Line XXX)
- missing_method_2 (來源: Line XXX)
...

⚠️ 目標有但來源沒有的方法（需確認是否保留）：
- extra_method_1 (目標: Line XXX)
...
```

---

### 【階段 2：關鍵方法逐行對比】

針對以下核心方法，執行逐行對比（**不可省略任何行**）：

#### 2.1 同步功能相關

**方法清單**：
- `supports_sync()` - 比對完整實現
- `get_parameter_interface()` - 比對返回值結構
- `update_from_shared_params()` - 比對每個 if/else 分支
- `_on_sync_driver_lap_toggled()` - 比對信號連接

**檢查步驟**：
1. 用 `read_file` 讀取來源方法（包含前後 10 行）
2. 用 `read_file` 讀取目標方法（包含前後 10 行）
3. 逐行比對差異（包括註釋、空格、print 語句）
4. 列出所有差異點

---

#### 2.2 參數更新相關

**方法清單**：
- `update_lap_parameters()` - **重點檢查**

**關鍵檢查點**：

**檢查點 1：參數變化檢查邏輯**
```python
# 搜索來源模組
grep_search: "params_changed.*="
includePattern: "[來源模組]_mdi.py"

# 搜索目標模組
grep_search: "params_changed.*="
includePattern: "[目標模組]_mdi.py"
```

**檢查點 2：數據載入觸發條件**
```python
# ⚠️ 關鍵陷阱：檢查是否有條件判斷
grep_search: "if params_changed:|跳過.*重載|skip.*reload"
includePattern: "[目標模組]_mdi.py"

# ✅ 來源模組應該是無條件執行
grep_search: "🚀 重新載入數據|self\.load_data"
includePattern: "[來源模組]_mdi.py"
```

**檢查點 3：時間軸模式設置**
```python
grep_search: "use_time_axis|set_time_axis_mode"
includePattern: "[來源模組]_mdi.py"
```

**檢查點 4：資訊標籤更新**
```python
grep_search: "_update_info_label"
includePattern: "[來源模組]_mdi.py"
```

---

#### 2.3 跨賽事比較相關

**方法清單**：
- `update_cross_event_comparison()` - 比對完整流程
- `_update_info_label()` - 比對標籤顯示邏輯

**檢查步驟**：
1. 讀取來源方法的完整實現（不省略任何 if/else）
2. 讀取目標方法的完整實現
3. 比對跨賽事判斷邏輯（`is_cross_event` 計算）
4. 比對標籤顯示格式（`vs` 格式）

---

#### 2.4 數據載入相關

**方法清單**：
- `load_data()` - 比對參數傳遞
- `_handle_data_loaded()` - 比對回調處理
- `_generate_telemetry_via_api()` - 比對 API 調用

**檢查重點**：
- 參數是否正確傳遞到 DataLoader
- 回調函數的信號連接是否正確
- API 調用的端點和參數是否一致

---

### 【階段 3：執行流程對比】

對於每個方法，**必須**比對以下 6 個層面：

#### 3.1 方法簽名
```python
# 檢查項目：
- 參數列表順序
- 參數默認值
- 類型註解（Type Hints）
- 返回值類型
```

#### 3.2 條件分支邏輯
```python
# 檢查項目：
- 每個 if 條件的判斷式
- 每個 elif 的條件
- 每個 else 分支的處理
- 嵌套 if 的完整性
```

#### 3.3 調試輸出
```python
# 檢查項目：
- 每個 print 語句的內容
- 日誌標記（[XXX_MDI]、[DEBUG] 等）
- emoji 符號（🚀、✅、❌ 等）
```

#### 3.4 方法調用鏈
```python
# 檢查項目：
- self.xxx() 方法調用
- 參數傳遞順序
- 關鍵字參數的名稱
```

#### 3.5 返回值邏輯
```python
# 檢查項目：
- return True/False 的條件
- return 語句的位置
- 是否有遺漏的 return
```

#### 3.6 異常處理
```python
# 檢查項目：
- try/except 的範圍
- 異常類型的捕獲
- 錯誤訊息的格式
```

---

### 【階段 4：關鍵差異檢查】

特別檢查以下**已知的陷阱**：

#### 4.1 數據重載邏輯 ⚠️ **最常見的錯誤**

**正確模式（來源模組）**：
```python
# ✅ 無條件執行數據載入
print(f"[XXX_MDI] 🚀 重新載入數據...")
success = self.load_data(
    year=self.current_year,
    race=self.current_race,
    # ... 其他參數
)
```

**錯誤模式（可能存在於目標模組）**：
```python
# ❌ 有條件判斷，導致模式切換時跳過重載
if params_changed:
    print(f"[XXX_MDI] 🔄 參數已變化，開始重載數據...")
    success = self.load_data(...)
else:
    print(f"[XXX_MDI] ℹ️ 參數未變更，跳過數據重載")  # ❌ 這就是問題！
    return True
```

**檢查指令**：
```bash
grep_search: "if params_changed:.*else.*跳過"
includePattern: "[目標模組]_mdi.py"
isRegexp: true
```

**如果發現此模式 → 必須移除！改為無條件執行**

---

#### 4.2 時間軸功能

**檢查清單**：
- [ ] `use_time_axis` 參數是否在方法簽名中
- [ ] 參數是否正確傳遞到 Chart Widget
- [ ] Chart Widget 是否有 `set_time_axis_mode()` 方法
- [ ] 是否在數據載入後調用時間軸設置

**檢查指令**：
```bash
# 檢查參數傳遞路徑
grep_search: "use_time_axis"
includePattern: "[目標模組]_mdi.py"

# 檢查 Chart Widget 方法
grep_search: "set_time_axis_mode"
includePattern: "[目標模組]/[目標模組]_chart_widget.py"
```

---

#### 4.3 同步功能

**檢查清單**：
- [ ] `sync_driver_lap_enabled` 屬性是否在 `__init__` 中初始化
- [ ] 同步勾選框的信號連接是否正確
- [ ] `_on_sync_driver_lap_toggled()` 方法是否存在
- [ ] 方法內是否正確處理勾選/取消邏輯

**檢查指令**：
```bash
# 檢查屬性初始化
grep_search: "sync_driver_lap_enabled.*="
includePattern: "[目標模組]_mdi.py"

# 檢查信號連接
grep_search: "toggled\.connect.*sync_driver_lap"
includePattern: "[目標模組]_mdi.py"
```

---

#### 4.4 資訊標籤更新

**檢查清單**：
- [ ] `_update_info_label()` 是否在 `update_lap_parameters` 中調用
- [ ] 是否在 `update_cross_event_comparison` 中調用
- [ ] 跨賽事模式的標籤格式是否為 "XXX vs YYY"
- [ ] 一般模式的標籤格式是否一致

**檢查指令**：
```bash
# 檢查調用時機
grep_search: "_update_info_label\(\)"
includePattern: "[目標模組]_mdi.py"
```

---

### 【階段 5：逐個修復差異】

對於發現的每個差異，**按照以下步驟修復**：

#### 步驟 1：讀取來源實現
```python
read_file(
    filePath="[來源模組]_mdi.py",
    offset=[方法起始行 - 5],
    limit=[方法行數 + 10]
)
```

#### 步驟 2：讀取目標實現
```python
read_file(
    filePath="[目標模組]_mdi.py",
    offset=[方法起始行 - 5],
    limit=[方法行數 + 10]
)
```

#### 步驟 3：執行替換
```python
replace_string_in_file(
    filePath="[目標模組]_mdi.py",
    oldString="[目標模組的舊實現（包含前後 5 行）]",
    newString="[來源模組的正確實現（調整模組名稱）]"
)
```

#### 步驟 4：報告修復
```markdown
【已修復】
- 方法名稱: update_lap_parameters
- 差異描述: 移除 'if params_changed' 條件判斷，改為無條件重載數據
- 修改位置: Line 1022-1065
- 修改內容: 複製 Speed 模組的無條件載入邏輯
```

---

### 【階段 6：驗證清單】

修復完成後，**必須確認**以下功能：

#### 6.1 同步功能驗證
- [ ] 勾選「車手與圈數同步」後，能從主視窗讀取參數
- [ ] 取消勾選後，能進入手動模式（顯示車手和圈數選擇器）
- [ ] 同步狀態變更時，日誌顯示正確的 `[SYNC_TOGGLED]` 訊息

#### 6.2 模式切換驗證
- [ ] 跨賽事比較模式能正常切換（不同 Year/Session）
- [ ] 一般模式能正常切換（相同 Year/Session）
- [ ] 模式切換時，資訊標籤顯示格式正確

#### 6.3 數據載入驗證
- [ ] 參數變更後能正確重載數據（**不跳過**）
- [ ] 參數相同但模式變更時，仍然重載數據
- [ ] 日誌顯示 `[XXX_MDI] 🚀 重新載入數據...`

#### 6.4 時間軸功能驗證
- [ ] 時間軸切換功能正常（Distance ↔ Time）
- [ ] `use_time_axis` 參數正確傳遞到所有層級
- [ ] Chart Widget 正確響應時間軸模式變更

#### 6.5 UI 顯示驗證
- [ ] 資訊標籤顯示正確的車手和圈數信息
- [ ] 跨賽事模式顯示 "XXX vs YYY" 格式
- [ ] 一般模式顯示單一賽事信息

---

### 【階段 7：測試計劃】

提供以下測試場景的**預期行為**：

#### 測試場景 1：勾選同步
```markdown
【操作】
1. 打開目標模組視窗
2. 點擊設定按鈕
3. 勾選「車手與圈數同步」
4. 點擊 OK

【預期日誌】
[SYNC_TOGGLED] 車手與圈數同步: 啟用
[SYNC_MODE] 車手與圈數同步已啟用，從主視窗讀取參數
[CALLING] _apply_driver_lap_settings() with main window params
[XXX_MDI] 🚀 重新載入數據...
[XXX_MDI] ✅ 圈速參數更新後數據重載成功

【預期結果】
- 目標模組自動使用主視窗的車手和圈數
- 數據正確重載
- 資訊標籤顯示正確
```

#### 測試場景 2：取消同步
```markdown
【操作】
1. 已勾選同步的狀態下
2. 打開設定對話框
3. 取消勾選「車手與圈數同步」
4. 手動選擇車手和圈數
5. 點擊 OK

【預期日誌】
[SYNC_TOGGLED] 車手與圈數同步: 停用
[MANUAL_MODE] 車手與圈數同步已停用，進入手動模式
[XXX_MDI] 🚀 重新載入數據...

【預期結果】
- 車手和圈數選擇器可用
- 使用手動設定的參數重載數據
```

#### 測試場景 3：跨賽事比較
```markdown
【操作】
1. 設定車手 1: 2025 Australia R
2. 設定車手 2: 2025 Japan R
3. 點擊 OK

【預期日誌】
[CROSS-EVENT] 檢測到跨賽事比較:
   車手 1: 2025 Australia R XXX 第X圈
   車手 2: 2025 Japan R YYY 第Y圈
[CROSS-EVENT] 調用 update_cross_event_comparison 方法

【預期結果】
- 資訊標籤顯示 "2025 Australia R vs 2025 Japan R"
- 跨賽事數據正確載入
```

#### 測試場景 4：參數相同但模式變更
```markdown
【情境】
- 當前狀態: 跨賽事比較 (2025 Australia R vs 2025 Japan R)
- 車手: VER vs LEC, 圈數: 1 vs 1

【操作】
1. 勾選同步
2. 主視窗參數: 2025 Brazil R, VER, Lap 1
3. 點擊 OK

【預期日誌】
[SYNC_MODE] 車手與圈數同步已啟用，從主視窗讀取參數
[SYNC_FIX] 🔒 已啟用同步，強制使用主視窗參數（單賽事模式）
[XXX_MDI] 🚀 重新載入數據...  ← ⚠️ 必須重載，不可跳過！

【預期結果】
- 即使車手和圈數相同，仍然重載數據
- 從跨賽事模式正確切換回一般模式
- 日誌不可出現「參數未變更，跳過數據重載」
```

---

## 📊 執行清單模板

**複製此清單到對話中，逐項確認**：

```markdown
【階段 0：原則確認】
- [ ] 已宣告反幻覺編碼五原則

【階段 1：方法列表】
- [ ] 已列出來源模組所有方法
- [ ] 已列出目標模組所有方法
- [ ] 已生成差異清單

【階段 2：關鍵方法對比】
- [ ] supports_sync
- [ ] get_parameter_interface
- [ ] update_from_shared_params
- [ ] update_lap_parameters
- [ ] update_cross_event_comparison
- [ ] _update_info_label
- [ ] load_data
- [ ] _handle_data_loaded
- [ ] _generate_telemetry_via_api

【階段 3：執行流程對比】
- [ ] 方法簽名一致
- [ ] 條件分支邏輯一致
- [ ] print 語句一致
- [ ] 方法調用鏈一致
- [ ] 返回值邏輯一致
- [ ] 異常處理一致

【階段 4：陷阱檢查】
- [ ] 無「if params_changed: ... else: 跳過重載」
- [ ] use_time_axis 參數正確傳遞
- [ ] sync_driver_lap_enabled 屬性存在
- [ ] _update_info_label() 正確調用

【階段 5：差異修復】
- [ ] 已修復所有發現的差異
- [ ] 每個修復都有報告

【階段 6：功能驗證】
- [ ] 同步功能正常
- [ ] 模式切換正常
- [ ] 數據載入正常
- [ ] 時間軸功能正常
- [ ] UI 顯示正確

【階段 7：測試場景】
- [ ] 測試場景 1 通過
- [ ] 測試場景 2 通過
- [ ] 測試場景 3 通過
- [ ] 測試場景 4 通過
```

---

## 🎯 快速啟動範例

**直接複製此範例，修改模組名稱即可使用**：

```markdown
【來源模組】Speed 模組
【目標模組】Gear 模組

請執行以下完整功能複製流程：

【階段 0：原則宣告】
請先宣告「反幻覺編碼五原則」

【階段 1：完整方法列表對比】
請列出 Speed 和 Gear 模組的所有方法差異

【階段 2-7：按範本執行】
請按照「0.複製範本.md」的完整流程執行
```

---

## ⚠️ 重要提醒

1. **不可省略任何步驟** - 每個階段都必須完整執行
2. **不可假設方法存在** - 必須用 grep_search 驗證
3. **不可跳過 else 分支** - 必須檢查所有條件分支
4. **不可忽略 print 語句** - 調試信息也要完全一致
5. **不可自行簡化邏輯** - 必須完整複製來源模組的實現

---

**使用此範本，確保每次功能複製都是完整且正確的！** 🎯

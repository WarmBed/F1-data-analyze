# F1T - 開發依賴清單 (Development Dependencies)
# 生成日期: 2026-04-04
#
# 安裝方式:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
# 包含內容:
#   - 測試框架
#   - 程式碼品質工具
#   - 除錯工具

# ── 生產依賴（包含）────────────────────────────────────────────────────────
-r requirements.txt

# ── 測試框架 ─────────────────────────────────────────────────────────────
pytest==8.4.0                 # 主要測試框架
pytest-cov==6.2.1             # 覆蓋率報告
pytest-qt==4.5.0              # PyQt5 GUI 測試支援
pytest-asyncio>=0.23.0        # 異步測試（FastAPI 端點）

# ── 程式碼品質 ───────────────────────────────────────────────────────────
ruff>=0.4.0                   # 快速 Linter + Formatter
black>=24.0.0                 # 程式碼格式化
isort>=5.13.0                 # import 排序

# ── 除錯工具 ─────────────────────────────────────────────────────────────
ipython>=8.0.0                # 互動式 Python Shell

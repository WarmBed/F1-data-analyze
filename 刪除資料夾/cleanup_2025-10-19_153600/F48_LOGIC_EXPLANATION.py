"""
F48 全車手直線速度與加速分析 - 完整邏輯說明
===========================================

## 📊 數據計算邏輯（2025-10-15 最終版本）

### 1️⃣ **每位車手最高速度邏輯**

#### 步驟 1: 確定參考直線段（由最快車手決定）
- 使用 **VER**（或最快車手）的最速圈
- 找到整圈的**最高速度點**
- 向前回推找到**直線段起點**（速度持續上升的起點）
- 定義：`參考範圍 = 起點位置 → 最高速度位置`
- 例如：Azerbaijan VER 的參考範圍 = **5285m - 5485m**（200m）

#### 步驟 2: 擴展範圍（允許個別差異）
- 核心範圍：參考範圍（5285-5485m）
- **擴展範圍：核心範圍 ±200m**（5085-5685m）
- 目的：允許車手在略微不同的位置達到最高速度

#### 步驟 3: 每位車手的最高速度測量
- 在該車手的**最速圈**遙測數據中
- 搜索**擴展範圍內**（5085-5685m）的所有速度點
- 取這個範圍內的**最高速度**作為該車手的最高速度
- 記錄該速度點的位置、時間、油門、DRS 狀態

**標註：**
- 如果最高速度點在核心範圍內（5285-5485m）：標註 `in_core_range: True`
- 如果在擴展範圍但不在核心範圍：標註 `in_core_range: False`，顯示為 [擴展範圍]

---

### 2️⃣ **加速性能計算邏輯（100 → 320 km/h）**

#### 步驟 1: 確定統一速度範圍（所有車手相同）✅ 新邏輯（2025-10-15）

**起始速度（從低往高找）：**
1. 對每位車手，計算錨點搜索範圍：`max_speed_pos - (track_straight - 100m)`
2. 在錨點搜索範圍內找到該車手的最小速度
3. 收集所有車手的最小速度，取**所有車手的最小值**
4. 向上取整到 10 km/h 倍數
5. **上限：200 km/h**

範例：
- 所有車手最小速度的最小值 = 145 km/h → 向上取整 → **150 km/h**
- 所有車手最小速度的最小值 = 187 km/h → 向上取整 → 190 km/h → 限制上限 → **200 km/h**

**終點速度（從高往低找）：**
1. 對每位車手，在擴展範圍（±200m）內找到最高速度
2. 收集所有車手的最高速度，取**所有車手的最小值**（確保所有人都能達到）
3. 向下取整到 10 km/h 倍數
4. **下限：200 km/h**

範例（Azerbaijan 2025）：
- 所有車手最高速度的最小值 = 324 km/h → 向下取整 → **320 km/h**
- 所有車手最高速度的最小值 = 198 km/h → 向下取整 → 190 km/h → 提升至下限 → **200 km/h**

#### 步驟 2: 智能搜索範圍（基於賽道特性）
**硬編碼賽道直線長度：**
```python
TRACK_STRAIGHT_LENGTHS = {
    "Monaco": 400,          # 最短直線
    "Singapore": 800,
    "Hungary": 600,
    "Azerbaijan": 2200,     # 超長直線（Baku）
    "Monza": 1100,
    "Spa": 850,
    # ... 其他賽道
}
```

**計算搜索起點：**
```
車手最高速度位置（例如 5485m）
    ↓
往前推算：5485 - (賽道直線長度 - 100m)
    ↓
Azerbaijan: 5485 - (2200 - 100) = 3385m（正確！）
Monaco: 5485 - (400 - 100) = 5185m（短直線）
Monza: 5485 - (1100 - 100) = 4385m（中等直線）
```

**重要**：硬編碼直線長度代表該賽道主直線段的**實際物理長度**！
- Azerbaijan (Baku)：2200m 超長直線（減去 100m 緩衝）
- Monaco：400m 最短直線（減去 100m 緩衝）
- 參考段 200m 只是用來定位最高速度區域，與加速搜索無關

**搜索範圍：**
```
起點：車手最高速度位置 - (硬編碼直線長度 - 100m)
終點：車手最高速度位置 + 200m
```

#### 步驟 3: 尋找加速起點（基於統一起始速度）✅ 新邏輯
在搜索範圍內，從最高速度點往前推：
1. 優先找：速度 ≤ **統一起始速度 + 10 km/h**（容差 ±10）
   - 例如：統一起始 = 150 km/h，尋找 ≤ 160 km/h 的點
2. 次優找：速度 ≤ **統一起始速度**
   - 例如：尋找 ≤ 150 km/h 的點
3. 回退找：搜索範圍內的**最小速度**（強制全車手模式）

#### 步驟 4: 尋找加速終點（基於統一終點速度）✅ 新邏輯
從起點往後搜索，找第一個 ≥ **統一終點速度** 的點：
- 例如：統一終點 = 320 km/h，尋找第一個 ≥ 320 km/h 的點
- 如果找到：使用該點
- 如果找不到（車手最高速度 < 統一終點）：使用**最高速度點**作為終點（強制全車手模式）

#### 步驟 5: 計算加速指標

**A. 加速時間（acceleration_time_100_300_seconds）**
```python
time_diff = time_end - time_start  # 秒
```
- Azerbaijan 範圍：19.96s - 24.08s
- **無上限限制**（之前有 20 秒限制，已移除）

**B. 加速距離（acceleration_distance_100_300_meters）**
```python
distance_diff = distance_end - distance_start  # 米
```
- 實際測量的加速段長度
- 約 400-800m（視賽道和車手而定）

**C. 平均加速度（avg_acceleration_ms2）** ✅ 新邏輯
```python
velocity_change = (統一終點速度 - 統一起始速度) / 3.6  # 轉換為 m/s
avg_acceleration = velocity_change / time_diff  # m/s²
```
- 單位：m/s²（米/秒²）
- 反映車手在直線段的平均加速能力
- 例如：(320 - 150) / 3.6 / 時間 = 平均加速度

---

### 3️⃣ **最高時速時間（time_to_max_speed）**

**定義：** 從加速起點（100 km/h）到達**最高速度點**所需的時間

**計算方式：**
```python
time_to_max = time_at_max_speed - time_at_start_point  # 秒
```

**與加速時間的區別：**
- **加速時間**：100 → **320 km/h**（統一終點）
- **最高時速時間**：100 → **實際最高速度**（可能 324-341 km/h）

例如 NOR（最高速度 341 km/h）：
- 加速時間（100→320）：22.52s
- 最高時速時間（100→341）：可能 23.5s（更長）

---

## 📈 完整數據流程圖

```
步驟 1: 選擇最快車手（VER）
    ↓
步驟 2: 找 VER 的最速圈
    ↓
步驟 3: 識別主直線段（5285m - 5485m）
    ↓
步驟 4: 定義擴展範圍（±200m）
    ↓
步驟 5: 確定統一速度範圍（100 → 320 km/h）
    ↓
步驟 6: 對每位車手執行：
    │
    ├─> 找最速圈
    │
    ├─> 在擴展範圍內找最高速度點 ────→ 最高速度
    │       │                          │
    │       └─> 記錄位置、時間 ────────→ 最高速度位置
    │
    ├─> 計算搜索起點（最高速度位置 - (直線長度 - 100m)）
    │
    ├─> 在搜索範圍內找 100 km/h 起點
    │
    ├─> 從起點找 320 km/h 終點
    │
    ├─> 計算加速時間（終點時間 - 起點時間）────→ 加速時間
    │
    ├─> 計算加速距離（終點位置 - 起點位置）────→ 距離
    │
    ├─> 計算平均加速度（速度變化 / 時間）─────→ 平均加速度
    │
    └─> 計算最高時速時間（最高速時間 - 起點時間）→ 最高時速時間
```

---

## 🎯 Azerbaijan 2025 實際範例

### VER（參考車手）
- 最高速度：**324 km/h** @ 5485m [核心範圍]
- 加速時間（100→320）：**24.081s**
- 搜索範圍：5485 - (2200 - 100) = **3385m** 開始（往前推 2100m！）
- 找到 100 km/h @ 約 3400m（Baku 超長直線起點）
- 找到 320 km/h @ 約 5450m
- 加速距離：約 2050m（完整 Baku 直線）
- 平均加速度：約 2.54 m/s²

### HAM（最快加速）
- 最高速度：**331 km/h** @ 5597m [擴展範圍]
- 加速時間（100→320）：**19.960s**（最快！）
- 搜索範圍：5597 - (2200 - 100) = **3497m** 開始
- 加速距離：約 2100m（完整 Baku 直線）
- 平均加速度：約 3.06 m/s²（最高）

### NOR（最高速度）
- 最高速度：**341 km/h** @ 5685m [擴展範圍]
- 加速時間（100→320）：**22.520s**
- 搜索範圍：5685 - (2200 - 100) = **3585m** 開始

---

## ✅ 關鍵設計原則

1. **最高速度**：反映車輛在主直線段的終端速度能力
2. **加速性能**：使用統一速度範圍（100→320）確保公平比較
3. **位置標準化**：所有車手在同一條直線段（±200m 容差）測量
4. **智能搜索**：根據賽道特性動態調整搜索範圍
5. **強制全車手**：降低要求確保所有車手都有數據（100%覆蓋率）

---

## 🔧 當前版本特性

✅ 階梯式統一速度範圍（320 km/h）
✅ 位置標準化（擴展範圍 ±200m）
✅ 智能搜索範圍（基於賽道硬編碼）
✅ 強制全車手模式（100% 覆蓋率）
✅ 取消時間差限制
✅ 完整的加速指標計算

---

生成時間：2025-10-15
版本：F48 v2.1 (API-ONLY + Smart Search)
"""

print(__doc__)
